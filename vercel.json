{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "DATABASE_URL=${DATABASE_URL:-postgresql://localhost/dummy} prisma generate && prisma db push --skip-generate --accept-data-loss && next build",
  "framework": "nextjs"
}
