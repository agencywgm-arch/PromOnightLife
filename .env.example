# ============================================================
#  NIGHTLIFE PARIS — MODÈLE DE CONFIGURATION
#  Copiez ce fichier en .env (local) ou collez ces variables
#  dans Vercel → Settings → Environment Variables (production).
#  Toutes sont optionnelles SAUF DATABASE_URL.
# ============================================================

# --- 1. BASE DE DONNÉES (obligatoire) -----------------------
# Base PostgreSQL gratuite. Crée-la sur https://neon.tech (sans carte bancaire),
# copie la "Connection string" (commence par postgresql://...).
DATABASE_URL="postgresql://user:password@ep-xxx.eu-central-1.aws.neon.tech/neondb?sslmode=require"

# --- 2. SÉCURITÉ (recommandé) -------------------------------
# Secret de signature des sessions (JWT). Génère-en un avec :  openssl rand -hex 32
AUTH_SECRET="remplace-moi-par-une-longue-chaine-aleatoire"

# --- 3. AGENT CARROUSEL INSTAGRAM ---------------------------
# 3a. PHOTOS — Clé Google Places (pour récupérer les photos des lieux).
#     Console : https://console.cloud.google.com → API "Places API" + "Maps Static"
#     ⚠️ Google demande un compte de facturation (carte), mais l'usage reste
#     dans le crédit gratuit de 200 $/mois.
GOOGLE_PLACES_API_KEY=""

# 3b. PUBLICATION — Agent Meta / Instagram (pour publier le carrousel généré).
#     Nécessite un compte Instagram PRO relié à une page Facebook + une app Meta.
#     Docs : https://developers.facebook.com/docs/instagram-api
META_ACCESS_TOKEN=""        # Access token longue durée de l'app Meta
META_IG_USER_ID=""          # ID du compte Instagram Business (ex : 17841400000000000)

# 3c. CANDIDATURES — Agent ManyChat (réception des inscriptions via DM Instagram).
#     Profil → Settings → API → "Your API Key"
MANYCHAT_API_KEY=""

# --- 4. WEBHOOKS (optionnel) --------------------------------
# Si défini, les webhooks /api/webhook/* exigent ce secret
# (header x-webhook-secret OU ?secret=...). Laisse vide pour les ouvrir.
WEBHOOK_SECRET=""
